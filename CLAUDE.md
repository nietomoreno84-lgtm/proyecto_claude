# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Estado actual

Este proyecto está en una etapa muy temprana: solo contiene un punto de entrada mínimo (`main.py`) y un `requirements.txt` vacío. Todavía no hay arquitectura, módulos ni dependencias que documentar. Actualiza esta sección a medida que el proyecto crezca.

## Entorno

- Python 3.12, entorno virtual en `.venv/`.
- Activar el entorno (PowerShell): `.venv\Scripts\Activate.ps1`
- Instalar dependencias: `pip install -r requirements.txt`

## Comandos

- Ejecutar la aplicación: `python main.py`
